#include "parser.h"

static int parse_binary_op( const struct token_entry* tokens, uint32_t n_tokens, struct node* noot) {

}

int parse ( const struct token_entry* tokens, uint32_t n_tokens, struct node* root ) {
    for (uint32_t i = 0; i < n_tokens; i++) {
        
    }
}
