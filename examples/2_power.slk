def power_2(k) {
    var i = 0
    var power = 1
    while( i < k ) {
        power = power * 2
        i = i + 1
    }

    return power
}

var n = input_number("insert n for 2^n to compute: ")
print("2 to the power of " + str(n) + " is: " + str(power_2(n)))

